# 2026-04-13 日志

## 今日主要成果

### 1. Continue/Spawn 决策引擎 ✅
- **代码**：`skills/continue-spawn-decision/`（decide.py + decision.ts）
- **集成**：AGENTS.md Section 3.4 完整接入
- **算法**：加权评分（文件重叠×0.4 + 概念重叠×0.3 + 工具重叠×0.2 + 消息重叠×0.1）
- **决策规则**：≥0.6 → continue，≤0.3 → spawn_fresh，verification 强制新 agent

### 2. 两层验证机制 ✅
- **设计文档**：`agents/research/two-layer-verification-design-2026-04-13.md`
- **Layer 1**：Worker 自验（build + 相关测试）
- **Layer 2**：独立验证者（从需求出发，不继承实现上下文）
- **失败处理**：needs_changes → @dev 修复 → 重跑 Layer 2；rejected → 汇报主人

### 3. Task 持久化插件 ✅
- **位置**：`extensions/session-task-persistence/`
- **Phase 1**：Polling 扫描 runs.sqlite，每30秒一次，已补录所有历史 task
- **Phase 2**：依赖 Gateway 内置 `openclaw tasks [list|show]`，不重复造轮子
- **Phase 3**：Memory 集成（task 结束后写入 `memory/types/project/session-{id}.md`）+ LRU 驱逐（保留50个，24小时前）
- **已验证**：task 完成 → 6秒内注册 → memory 文件立即写入

### 4. ClawHub Skills 安装 ✅
- 已安装 8 个 skill（tavily, clawhub, coding-agent, healthcheck, mcporter, node-connect, session-logs, skill-creator 等）
- 安装目录：`~/.agents/skills/`

### 5. AGENTS.md 大升级 ✅
- Section 3 全面重写（Coordinato 模式、Continue/Spawn 决策引擎、两层验证机制）

## 今日教训

### 教训1：不要护犊子
- **情境**：验证 CLI 时发现 bug（`listSessionTasks` 在插件中扫描为空），但我先说"非本插件 bug"
- **问题**：不诚实，损害信任
- **原则**：发现 bug 直接承认，不找借口，优先说清实际情况

### 教训2：Gateway 内置功能优先
- **情境**：写了完整的 CLI 插件，后来发现 Gateway 已有功能更强的 `openclaw tasks`
- **原则**：先调研现有能力，再决定是否自建

### 教训3：Polling > Hook
- **情境**：hook 体系在部分场景是死定义（`subagent_spawned` 等事件未真正触发）
- **方案**：polling 直接扫 sqlite 更可靠

## 明日待办
- Cron 自动化修复（晨间简报/晚间总结 timed_out 问题）
- 知识图谱补全（将设计文档写入 knowledge/）
- SOUL.md 进一步演进（人格细化）
