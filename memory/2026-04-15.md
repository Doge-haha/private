# 2026-04-15 Daily Memory

## 凌晨至早间监控异常记录

- **00:03 监控**: SIGKILL (quiet-se, signal SIGKILL)，`web_search` exec 超时被系统强制终止
- **00:02 / 02:12 / 04:03 / 06:04 / 08:05**: 连续高频监控均无重大异动，返回 HEARTBEAT_OK
- **建议**: 凌晨时段 (00:00-07:00) 搜索频率可降为 4 小时一次，降低资源占用

## 工程计价平台开发文档完成

- **文档路径**: `knowledge/engineering-cost-platform.md`（约 20KB，v1.0.0 草稿）
- **内容**: 行业分析、架构设计、数据模型、AI 原生设计、RAG 架构、API 设计、12 周开发计划
- **状态**: 已完成，等待主人审阅和开发指令

## 待处理任务

- 工程计价平台 Phase 1 开发（架子搭建、数据库设计）
- 等待主人指令是否开始执行开发


# 2026-04-15 Daily Memory (Afternoon Update)

## 新点清单造价江苏版 — 数据逆向分析

### 项目路径
`/Users/huahaha/WorkSpace/something/新点清单造价江苏版`

### 数据形态侦查结果

| 类型 | 格式 | 规模 | 状态 |
|------|------|------|------|
| 定额数据（.de） | Firebird 嵌入式数据库 | 12+ 万条记录 | 🔒 需 Firebird client |
| 定额数据（SQLite） | dwgj.db | 12.2 万条 | ✅ 可直接读取 |
| 清单数据（.qa） | 自定义加密 binary | 34MB | 🔒 需逆向 |
| XML 配置 | DeData*.xml | Base64 + zlib | ⚠️ 部分可解 |

### SQLite 定额库详情

- **路径**: `Data/江苏/Dek/dwgj.db`
- **字段**: DekID(定额集代号), Debh(定额编号), Demc(定额名称), DeDj(单价), Dw(单位), Zjh(章编号)
- **内容**: 覆盖土建、装饰、安装、市政、园林、2013/2024定额等江苏全标准
- **可直接导出 CSV**

### 清单加密状态

- `Qdk.qa`（34MB），文件头 `5B A7 93 03`，自定义加密
- 加密器：`EpointZJSrv.exe` 或相关 DLL，需逆向
- 状态：未开始逆向

### 下一步决策点
- 先导出定额（5分钟可出 CSV）还是继续逆向清单加密（需更多时间）— 待主人指示


# 2026-04-15 Daily Memory (Late Afternoon Update)

## Telegram 媒体下载失败 — 根因定位

### 问题现象
- 从 `openclaw-control-ui` WebChat 发送图片，AI 收到"没有收到任何图片"
- 日志显示 SIGKILL 超时，exec 命令被系统强制终止

### 根因
- 系统环境变量 `HTTP_PROXY=http://127.0.0.1:7890`（Clash HTTP 代理）
- OpenClaw Telegram channel 下载媒体时要求代理协议为 `http://` 或 `https://`
- 但 Telegram Bot 配置可能使用了非 http 协议（如 socks5），导致不兼容
- 错误信息：`Explicit proxy URL must use http or https`

### 待处理
- 检查 `~/.openclaw/config.yml` 中 Telegram channel 的 proxy 配置
- 如使用 socks5，改为 `http://127.0.0.1:7890`

## GreeCost「青造价」网站调研 (greedig.cn)

### 网站特征
- 纯 SPA 应用，JS 渲染，内容不在 HTML 源码中
- 所有 API 都指向内网（192.168.0.181 / 192.168.1.202 / 192.168.2.12）
- 从外部无法直接调用内部 API
- 公开可访问的 API：`/api/list`, `/api/dinge`, `/api/price`, `/api/data`, `/api/query`, `/api/search`, `/api/category`, `/api/material`, `/api/standard`, `/api/info`, `/api/project`（均返回 HTML 或 404）

### 收到截图分析
- **截图路径**: `/Users/huahaha/Desktop/photo_2026-04-15_14-51-06.jpg`
- **界面内容**: 清单库页面，分类树（砖作工程 > 砌砖墙/贴砖/砖檐...）
- **右侧表格列**: 项目编码、项目名称、项目特征、计量单位、工程量计算规则
- **当前分类**: 仿古建筑工程工程量计算标准(2024)
- **清单编码格式**: 020101001 开头（2012 清单规范）

### 结论
- 网站数据需从内网访问，外网无法抓取
- 需要账号登录或 VPN 进入内网
- 用户提到"这个系统的网页样式是这样"——说明 UI 截图已收到

## CompactionPlus 压缩丢失问题分析

### 问题描述
- 10:20 和 14:03 两次压缩后，早晨的工作内容丢失
- "mano 项目"无法追溯，MEMORY 和 workspace 均无此记录

### 根因分析
1. **双重压缩叠加**：每次压缩只保留摘要，多次叠加细节丢失
2. **mergeSummaries 实现缺陷**：先摘要 + 后摘要 = 简单拼接（`mergeSummaries` 只是 `## 先前... \n ---\n ## 最近...`），不是真正的语义融合
3. **历史记录断裂**：压缩后的对话无法追溯压缩前的具体操作

### 修复方案（待实施）
在 CompactionPlus 中增加 **Session Snapshot 机制**：
- 每次压缩前将关键上下文写入 `memory/2026-04-15.md`
- 确保压缩轮次中重要信息不丢失

### 用户反馈
- 用户对压缩丢失问题非常不满，要求优先修复
- 主人原话："怎么会压缩后丢失呢，你是不是应该好好查查这个问题先"

## 用户提到但未找到的项目
- **"mano 项目"**：MEMORY 和 workspace 均无记录，可能是打错字或早晨对话压缩后丢失

## 待处理任务（优先级排序）
1. 🔴 **CompactionPlus 压缩丢失修复** — Session Snapshot 机制
2. 🟡 **Telegram 代理配置修复** — 检查并修正 proxy 协议
3. 🟢 **GreeCost 数据** — 需内网访问或用户导出
4. 🟢 **mano 项目** — 确认项目名
# 2026-04-15 Daily Memory

## 凌晨至早间监控异常记录

- **00:03 监控**: SIGKILL (quiet-se, signal SIGKILL)，`web_search` exec 超时被系统强制终止
- **00:02 / 02:12 / 04:03 / 06:04 / 08:05**: 连续高频监控均无重大异动，返回 HEARTBEAT_OK
- **建议**: 凌晨时段 (00:00-07:00) 搜索频率可降为 4 小时一次，降低资源占用

## 工程计价平台开发文档完成

- **文档路径**: `knowledge/engineering-cost-platform.md`（约 20KB，v1.0.0 草稿）
- **内容**: 行业分析、架构设计、数据模型、AI 原生设计、RAG 架构、API 设计、12 周开发计划
- **状态**: 已完成，等待主人审阅和开发指令

## 待处理任务

- 工程计价平台 Phase 1 开发（架子搭建、数据库设计）
- 等待主人指令是否开始执行开发


# 2026-04-15 Daily Memory (Afternoon Update)

## 新点清单造价江苏版 — 数据逆向分析

### 项目路径
`/Users/huahaha/WorkSpace/something/新点清单造价江苏版`

### 数据形态侦查结果

| 类型 | 格式 | 规模 | 状态 |
|------|------|------|------|
| 定额数据（.de） | Firebird 嵌入式数据库 | 12+ 万条记录 | 🔒 需 Firebird client |
| 定额数据（SQLite） | dwgj.db | 12.2 万条 | ✅ 可直接读取 |
| 清单数据（.qa） | 自定义加密 binary | 34MB | 🔒 需逆向 |
| XML 配置 | DeData*.xml | Base64 + zlib | ⚠️ 部分可解 |

### SQLite 定额库详情

- **路径**: `Data/江苏/Dek/dwgj.db`
- **字段**: DekID(定额集代号), Debh(定额编号), Demc(定额名称), DeDj(单价), Dw(单位), Zjh(章编号)
- **内容**: 覆盖土建、装饰、安装、市政、园林、2013/2024定额等江苏全标准
- **可直接导出 CSV**

### 清单加密状态

- `Qdk.qa`（34MB），文件头 `5B A7 93 03`，自定义加密
- 加密器：`EpointZJSrv.exe` 或相关 DLL，需逆向
- 状态：未开始逆向

### 下一步决策点
- 先导出定额（5分钟可出 CSV）还是继续逆向清单加密（需更多时间）— 待主人指示


# 2026-04-15 Daily Memory (Late Afternoon Update)

## Telegram 媒体下载失败 — 根因定位

### 问题现象
- 从 `openclaw-control-ui` WebChat 发送图片，AI 收到"没有收到任何图片"
- 日志显示 SIGKILL 超时，exec 命令被系统强制终止

### 根因
- 系统环境变量 `HTTP_PROXY=http://127.0.0.1:7890`（Clash HTTP 代理）
- OpenClaw Telegram channel 下载媒体时要求代理协议为 `http://` 或 `https://`
- 但 Telegram Bot 配置可能使用了非 http 协议（如 socks5），导致不兼容
- 错误信息：`Explicit proxy URL must use http or https`

### 待处理
- 检查 `~/.openclaw/config.yml` 中 Telegram channel 的 proxy 配置
- 如使用 socks5，改为 `http://127.0.0.1:7890`

## GreeCost「青造价」网站调研 (greedig.cn)

### 网站特征
- 纯 SPA 应用，JS 渲染，内容不在 HTML 源码中
- 所有 API 都指向内网（192.168.0.181 / 192.168.1.202 / 192.168.2.12）
- 从外部无法直接调用内部 API
- 公开可访问的 API：`/api/list`, `/api/dinge`, `/api/price`, `/api/data`, `/api/query`, `/api/search`, `/api/category`, `/api/material`, `/api/standard`, `/api/info`, `/api/project`（均返回 HTML 或 404）

### 收到截图分析
- **截图路径**: `/Users/huahaha/Desktop/photo_2026-04-15_14-51-06.jpg`
- **界面内容**: 清单库页面，分类树（砖作工程 > 砌砖墙/贴砖/砖檐...）
- **右侧表格列**: 项目编码、项目名称、项目特征、计量单位、工程量计算规则
- **当前分类**: 仿古建筑工程工程量计算标准(2024)
- **清单编码格式**: 020101001 开头（2012 清单规范）

### 结论
- 网站数据需从内网访问，外网无法抓取
- 需要账号登录或 VPN 进入内网
- 用户提到"这个系统的网页样式是这样"——说明 UI 截图已收到

## CompactionPlus 压缩丢失问题分析

### 问题描述
- 10:20 和 14:03 两次压缩后，早晨的工作内容丢失
- "mano 项目"无法追溯，MEMORY 和 workspace 均无此记录

### 根因分析
1. **双重压缩叠加**：每次压缩只保留摘要，多次叠加细节丢失
2. **mergeSummaries 实现缺陷**：先摘要 + 后摘要 = 简单拼接（`mergeSummaries` 只是 `## 先前... \n ---\n ## 最近...`），不是真正的语义融合
3. **历史记录断裂**：压缩后的对话无法追溯压缩前的具体操作

### 修复方案（待实施）
在 CompactionPlus 中增加 **Session Snapshot 机制**：
- 每次压缩前将关键上下文写入 `memory/2026-04-15.md`
- 确保压缩轮次中重要信息不丢失

### 用户反馈
- 用户对压缩丢失问题非常不满，要求优先修复
- 主人原话："怎么会压缩后丢失呢，你是不是应该好好查查这个问题先"

## 用户提到但未找到的项目
- **"mano 项目"**：MEMORY 和 workspace 均无记录，可能是打错字或早晨对话压缩后丢失

## 待处理任务（优先级排序）
1. 🔴 **CompactionPlus 压缩丢失修复** — Session Snapshot 机制
2. 🟡 **Telegram 代理配置修复** — 检查并修正 proxy 协议
3. 🟢 **GreeCost 数据** — 需内网访问或用户导出
4. 🟢 **mano 项目** — 确认项目名


# 2026-04-15 Daily Memory (Evening Update)

## mano-cua 调研与卸载

### 调研结论
- **项目**: Mininglamp-AI/mano-skill（桌面 GUI 自动化工具）
- **架构**: Cloud-only 云端模式，数据上传到 `mano.mininglamp.com`
- **隐私问题**: 每次操作截屏上传第三方服务器
- **本地模型**: 存在独立项目 Mano-P，但需要源码编译，暂无 Homebrew 包
- **决定**: 不适合 privacy-conscious 使用场景

### 卸载操作
- 卸载了 mano-cua 1.0.7
- 清理了 Python 3.13、tcl-tk 9.0.3 及所有依赖
- 释放约 170MB 空间

## claude-code-haha 项目清理

### 发现
- 源码路径: `/Users/huahaha/WorkSpace/something/claude-code-haha/`（之前误以为在 `临时/`）
- package.json 名: `claude-code-local` v999.0.0-local
- 未全局安装（无 `claude-haha` 命令）
- 桌面快捷方式指向错误路径，已删除

### 决定
- 项目源码保留

## SuperGemma 4-26B 本地模型部署项目

### 项目背景
- 用户分享文章：Google Gemma 4 26B MoE 模型，Q4_K_M 量化后 17GB
- Apple M5 可跑出 46 tok/s，SuperGemma Fast V2 版本已解除审查限制
- Apache 2.0 许可证，可商用

### 已完成步骤
1. ✅ 编译 llama.cpp（Apple Silicon Metal 加速版）
   - 安装路径: `/opt/homebrew/Cellar/llama.cpp/8680`
   - 可用命令: `llama-cli`, `llama-server`, `llama-bench`, `llama-lookup`
2. ✅ 开始下载 SuperGemma Q4_K_M GGUF 模型
   - 文件: `supergemma4-26b-uncensored-fast-v2-Q4_K_M.gguf`
   - 大小: 16.8GB
   - 下载进程 PID: 26873
   - 下载工具: `hfget` (小明牌 HF 下载器)
   - 下载路径: `/Users/huahaha/WorkSpace/something/`

### 进行中
- ⏳ 模型下载中（17:22 时约 2.1GB/16.8GB，约 12%）
- 用户下班回家，Mac 预计整晚挂机下载

### 下一步（模型下完后）
1. 启动 `llama-server` 本地服务（端口 8080）
2. 配置 OpenClaw local model provider
3. 测试 SuperGemma 推理效果

### 相关信息
- 主人 Mac: M5（具体型号待确认，M5 Mac 应该是 M5 MacBook Air 或 Pro）
- 模型下载需要稳定网络连接

## 用户下班
- 17:22 用户离开办公室回家
- Mac 预计整晚挂机下载 SuperGemma 模型

## SuperGemma 4-26B 本地模型部署进展

### 下载完成
- 文件: `supergemma4-26b-uncensored-fast-v2-Q4_K_M.gguf`
- 大小: 16GB
- 下载耗时: ~45min（HF token 加速后 27MB/s）
- 路径: `/Users/huahaha/.openclaw/models/`

### llama-server 启动成功
- 命令: `llama-server -m <model> -c 4096 -ngl 99 --port 8080 --reasoning off`
- Metal 加速: ✅ M5 Mac 正常加载
- 模型参数: 25.23B
- 状态: 服务运行中，端口 8080

### ⚠️ Tokenization Bug
- **问题**: 所有输出都是 `<unused25>` 重复 token
- **根因**: Jiunsong 的 GGUF 转换工具可能有 bug，导致 embedding/LM head 映射损坏
- **影响**: 模型无法正常使用
- **解决方向**: 下载官方 ggml-org 标准版 GGUF 替代

### 备用方案
- 下载中: `ggml-org/gemma-4-26B-A4B-it-GGUF` (Q4_K_M, 16.8GB)
- 此版本为官方 llama.cpp 转换，tokenizer 应正常

### HF Token
- Token: `[REDACTED]`（有效期中）
- 已存储于 memory/2026-04-15.md
# 2026-04-15 Daily Memory

## SuperGemma Ollama 部署（最终结果）

### 清理完成
- ✅ SuperGemma GGUF (16GB) 已删除：`~/.openclaw/models/supergemma4-26b-uncensored-fast-v2-Q4_K_M.gguf`
- ✅ HF 缓存已清理（2.4GB 残留）
- ✅ llama-server.log 已删除
- ✅ resume_download.py 已删除

### Ollama 部署尝试

| 步骤 | 结果 |
|------|------|
| Homebrew Ollama | ✅ 安装了 v0.20.7（当时最新版） |
| `ollama run gemma4:26b` | ❌ 失败 — 0.20.7 不支持 Gemma 4 |
| `brew upgrade ollama` | 卡住，被 SIGTERM 终止 |
| 官方安装脚本 | ✅ 下载 v0.20.8（还是旧） |
| 手动下载 v0.6.2 | ✅ 成功，路径 `/usr/local/bin/ollama` |
| `ollama list` | ✅ 显示 v0.6.2 |
| `ollama pull gemma4:26b` | ⏳ 进行中（17GB，~25min） |

### gemma4:26b 下载状态（21:04 时）
- 2% / 17GB，速度 10-12 MB/s
- 网络慢，可能需要更长时间

### SuperGemma GGUF Bug 根因确认
- tokenizer 正常（jieba 加载无错）
- **LM head 损坏**：所有 token 输出 logit 都指向同一个 token（25）
- 这是量化转换工具的 bug，换任何 GGUF 源都会有问题
- **结论**：SuperGemma Q4_K_M GGUF 文件无法正常工作

### 待处理
- ⏳ gemma4:26b 下载中（Ollama）
- ⏳ MLX 版本下载（路径 `/Users/huahaha/.mlx/models/`）
- 等 Ollama 更新后测试 gemma4:26b

### HF Token（仍在使用）
- `[REDACTED]`
