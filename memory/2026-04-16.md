# 2026-04-16 记忆

## MLX 本地模型折腾记录

### SuperGemma4-26B-Abliterated-Multimodal MLX 4bit ✅

- **状态**: 下载完成，验证可用
- **路径**: `/Users/huahaha/Library/Application Support/mlx-lm/models/Jiunsong/supergemma4-26b-abliterated-multimodal-mlx-4bit/`
- **内存占用**: ~14.5GB / 24GB MacBook Pro
- **速度**: 84 tok/s（文字），40 tok/s（多模态）
- **验证命令**: `python3 -m mlx_vlm.generate --model <path> --prompt "hello"`
- **多模态测试**: `python3 -m mlx_vlm.generate --model <path> --prompt "red" --image <img.jpg>`

### 踩坑记录

1. **8bit 版本内存不够**: 24GB Mac 会 OOM，4bit 才够用
2. **下载极不稳定**: mlx_lm fetch 频繁中断，文件丢失。用手动 `curl -L -o` 下载更稳定
3. **文字输出乱码**: `----` 重复是 `<pad>` token，abliterated 模型正常行为
4. **HF_TOKEN**: 已配置在脚本中 `HF_TOKEN=[REDACTED]`

### mlx_vlm server 测试

- Server 可启动但长时间运行会被 SIGKILL（超时机制）
- `/health` 端点返回 200
- `/v1/chat/completions` 可用
- PID: 41332 (已退出)

### 下次建议

- 如果主要用文字，尝试 `Jiunsong/supergemma4-26b-abliterated` 纯文字版
- 如果要 4bit 多模态，现在这个已经可用
- model-00002-of-00003.safetensors 约 4.9GB，下载最慢

---

## OpenClaw 理事会进展

- Continue/Spawn 决策引擎已集成到委派流程
- 两层验证机制（Layer 1 自验 + Layer 2 独立验证）已建立
- 哨兵巡检正常，无异动

---

## agency-agents 安装（2026-04-17）

- 来源：`https://github.com/msitarzewski/agency-agents`
- 安装路径：`~/.openclaw/agency-agents/`
- 共 184 个专业 Agent 人设
- 索引已生成：`memory/types/reference/agent-index.md`
- 使用方式：委派任务时加载对应人设的 SOUL.md + AGENTS.md
- 典型场景：backend-architect、ai-engineer、security-engineer 等

---

## Java 环境安装（2026-04-17）

- OpenJDK 21.0.10（Homebrew）
- Gradle 9.4.1
- Maven 3.9.14
- 需要手动：`source ~/.zshrc`
